# Asking the user to input a two digit number and then it is adding the two digits together.
two_digit_number = input("Type a two digit number: ")

print (int(two_digit_number[0]) + int(two_digit_number[1]))